package application;
import java.util.Scanner;

public class Usuario {

    String nome;

    public int dados(){
        //escreve no arquivo a seguinte frase "usuário anonimo".
        //retorna 1 caso encontre o usuario no arquivo e 0 caso não encontre ou o usuario ou o arquivo.
        return 0;
    }
    public int dados(String nome) {
        //Escreve no arquivo o nome do usuário.
        //retorna 1 caso encontre o usuario no arquivo e 0 caso não encontre ou o usuario ou o arquivo.
        return 1;
    }
    public String getNome (){
        return nome;
    }
}
